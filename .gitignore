# build output — regenerate with `node build.js`
dist/

# node (project is stdlib-only, but keep it clean if someone adds tooling)
node_modules/
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# local agent and design-tool runtime data
.openclaude/
.superpowers/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# reference material (source PDFs the designs were derived from) — not part of the framework
/reference/
